//upload to remote server
git status
git add .
git commit -m "此次更新的敘述"
git push
git status


//reset to early version
git log --oneline //看之前上傳的checksum '384ddd4'
git reset --hard 384ddd4 //回到版本384ddd4，切記回到之前版本別用 git push -f，會讓遠端的資料庫也被返回 這樣就無法再回來到最新的版本了
./go.oru make vranstack 
